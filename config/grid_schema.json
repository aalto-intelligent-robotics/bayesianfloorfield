{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/product.schema.json",
  "title": "Configuration for grid map buidling",
  "description": "Configuration of grid map building",
  "type": "object",
  "properties": {
    "date": {
      "description": "Date of the file creation",
      "type": "string",
      "default": {}
    },
    "creator": {
      "description": "Name of the creator of the file.",
      "type": "string",
      "default": {}
    },
    "input_file": {
      "description": "path to a text file containing curated data",
      "type": "string",
      "default": {}
    },
    "input_description": {
      "type": "string",
      "description": "Free text for describing input data",
      "default": {}
    },
    "chunk_size": {
      "type": "integer",
      "description": "Size of the chunk to be processed when working with large files. If set to 0 files will be processed as a whole.",
      "default": 0
    },
    "occ_map_file": {
      "type": "string",
      "description": "Path to yaml file containing ros like information about the map",
      "default": {}
    },
    "output_file": {
      "type": "string",
      "description": "Path to the output file.",
      "default": {}
    }
  },
  "additionalProperties": false,
  "default": {}
}