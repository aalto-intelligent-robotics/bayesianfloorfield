{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://example.com/product.schema.json",
    "title": "Local Settings",
    "description": "Configuration of local folders and other settings",
    "type": "object",
    "properties": {
        "dataset_folder": {
            "description": "Folder containing the dataset.",
            "type": "string",
            "default": ""
        },
        "pickle_folder": {
            "description": "Folder containing the pickle model dumps.",
            "type": "string",
            "default": ""
        }
    }
}